# Agent instructions

This repository contains configuration for two Kubernetes clusters, both running **K3s**:

- `calcifer-cloud`, hosted on a remote VPS.
- `calcifer-home`, hosted on a server in the local network.

Keep changes scoped to the intended cluster and do not commit credentials or other secrets.
