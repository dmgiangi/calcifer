apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-assistant
  namespace: home-assistant
spec:
  secretName: home-assistant-tls
  dnsNames:
    - home.calcifer.tech
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-azure
