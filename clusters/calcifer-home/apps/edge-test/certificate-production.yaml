apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: edge-test-production
  namespace: edge-test
spec:
  secretName: edge-test-production-tls
  dnsNames:
    - edge-test.calcifer.tech
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-azure
