apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: edge-test
  namespace: edge-test
spec:
  endpoints:
    - dnsName: edge-test.calcifer.tech
      recordTTL: 60
      recordType: A
      targets:
        - 192.168.0.102
