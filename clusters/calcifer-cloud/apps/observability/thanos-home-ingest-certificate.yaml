apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: thanos-home-ingest
  namespace: monitoring
spec:
  secretName: thanos-home-ingest-tls
  dnsNames:
    - thanos-ingest.calcifer.tech
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-azure
