apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traces-home-ingest
  namespace: monitoring
spec:
  secretName: traces-home-ingest-tls
  dnsNames:
    - traces-ingest.calcifer.tech
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-azure
