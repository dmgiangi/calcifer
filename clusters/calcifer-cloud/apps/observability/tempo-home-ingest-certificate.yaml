apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tempo-home-ingest
  namespace: monitoring
spec:
  secretName: tempo-home-ingest-tls
  dnsNames:
    - tempo-ingest.calcifer.tech
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-azure
