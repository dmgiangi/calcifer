apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: calcifer-tech
  namespace: nginx-test
spec:
  secretName: calcifer-tech-tls
  issuerRef:
    name: letsencrypt-staging-azure
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - calcifer.tech
