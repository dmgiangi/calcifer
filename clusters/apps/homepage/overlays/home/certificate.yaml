apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage
  namespace: homepage
spec:
  secretName: homepage-tls
  issuerRef:
    name: letsencrypt-production-azure
    kind: ClusterIssuer
  dnsNames:
    - calcifer.tech
